import { friendSchema } from "./friend";
import { messageSchema } from "./message";
import { userSchema } from "./user";

export const schema = {
    friendSchema,
    userSchema,
    messageSchema,
};
